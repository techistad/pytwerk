[project]
name = "my_app"
version = "0.1.0"
description = "My App built with PyTwerk"
requires-python = ">=3.11"
dependencies = [
  "flask>=3.0.0,<4.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
